#! /bin/bash

set -euox pipefail

EMACS_SSH_HOST=${EMACS_SSH_HOST:-ursinia@ursinia.net}
EMACS_SSH_PATH=${EMACS_SSH_PATH:-/code}

if [[ $# -gt 0 ]]; then
    EMACS_SSH_PATH=$@
    shift
fi

emacs -nw /ssh:${EMACS_SSH_HOST}:${EMACS_SSH_PATH}

